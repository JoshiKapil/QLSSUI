# QLSS New Version

Angular 14 migration of the QLSS static HTML template.

## Commands

```bash
npm install
npm start
npm run build:prod
```

The application uses lazy-loaded feature modules for all migrated pages and keeps source assets under `src/assets`.